package Controller;

import java.io.IOException;

import Model.AESModel;

public class VCESController {

	public static void main(String args[]) throws IOException{
		// Super chiant à utiliser, il faut changer la config de compilation à chaque utilisation. 
		AESModel aes = new AESModel(args);
		
	}
}
